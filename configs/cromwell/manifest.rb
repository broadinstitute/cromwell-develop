# Instructions for configure.rb script
# To render .ctmpl files, include "render "file.ctmpl"
# Can also render secrets directly from vault with "copy_secret" and "copy_secret_from_path" (see configure.rb)
